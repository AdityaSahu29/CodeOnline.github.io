<?xml version="1.0" encoding="utf-8"?>
<main>
	<heading1>C Constants</heading1>
	<text>
		<heading2>Constants</heading2>
		<para>If you don't want others (or yourself) to change existing variable values, you can use the const keyword.This will declare the variable as "constant", which means unchangeable and read-only</para>
		<list>
			<li_title>Notes On Constants</li_title>
			<item>You should always declare the variable as constant when you have values that are unlikely to change</item>
			<item>When you declare a constant variable, it must be assigned with a value</item>
			<item>Another thing about constant variables, is that it is considered good practice to declare them with uppercase. It is not required, but useful for code readability and common for C programmers</item>
		</list>
		<para> </para>
	</text>
	<editor>
		<title>Example</title>
		<dis> </dis>
		<syntax><![CDATA[#include <stdio.h>

int main() {
  const int minutesPerHour = 60;
  const float PI = 3.14;
  return 0;
}]]></syntax>
	</editor>
</main>