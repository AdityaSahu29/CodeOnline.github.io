<?xml version="1.0" encoding="utf-8"?>
<main>
	<heading1>C Operators</heading1>
	<text>
		<heading2>Operators</heading2>
		<para>Operators are used to perform operations on variables and values.In the example below, we use the + operator to add together two values.Although the + operator is often used to add together two values, like in the example above, it can also be used to add together a variable and a value, or a variable and another variable</para>
		<list>
			<li_title>C divides the operators into the following groups:</li_title>
			<item>Arithmetic operators</item>
			<item>Assignment operators</item>
			<item>Comparison operators</item>
			<item>Logical operators</item>
			<item>Bitwise operators</item>
		</list>
		<para> </para>
	</text>
	<editor>
		<title>Example</title>
		<dis> </dis>
		<syntax><![CDATA[#include <stdio.h>

int main() {
  int sum1 = 100 + 50;        // 150 (100 + 50)
  int sum2 = sum1 + 250;      // 400 (150 + 250)
  int sum3 = sum2 + sum2;     // 800 (400 + 400)
  printf("%d\n", sum1);
  printf("%d\n", sum2);
  printf("%d\n", sum3);
  return 0;
}]]></syntax>
	</editor>
</main>